# FolioFrame Logo Status

Status: blocked pending approved final production logo master.

The current scaffold uses a temporary text-only FolioFrame lockup in `components/BrandLockup.tsx`.

The lockup renders:

- FolioFrame
- Checkout-to-customer delivery

No icon, mark, generated symbol or final brand visual is included.

## Rules

- Do not create a new logo.
- Do not redraw a logo.
- Do not generate logo artwork.
- Do not replace the text-only lockup until an approved final production master is provided.
- Keep logo references documented as pending.

## Implementation Note

The text-only component includes a TODO comment so future implementation work can replace it only when final brand approval exists.
